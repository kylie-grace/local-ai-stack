# secrets — never commit
.env

# CLIProxyAPI / HolyClaude — local-only coding-workstation experiment, not part
# of the public stack. Files kept on disk but never committed.
cliproxyapi/
holyclaude/
docker-compose.holyclaude.yml

# SearXNG — limiter DB is auto-generated at runtime, not config
# settings.yml contains secret_key — gitignored, use settings.yml.example as template
searxng/settings.yml
searxng/limiter.toml
searxng/favicons.db
searxng/settings.yml.new

# ephemeral artifacts
*.db
*.log
*.pid
__pycache__/
*.pyc

# macOS metadata
.DS_Store

# personal/session artifacts — not for public repo
Claude export/
PLAN.md
copilot-repairs.md

# old CLIProxyAPI helper — references personal account info
scripts/sync-anthropic-token.sh

